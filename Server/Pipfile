[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.0"
flask-sqlalchemy = ">=3.1.1"
flask-migrate = ">=4.0.7"
flask-jwt-extended = ">=4.6.0"
flask-cors = ">=4.0.0"
flasgger = ">=0.9.7.1"
python-dotenv = ">=1.0.1"
werkzeug = ">=3.0.0"

[dev-packages]

[requires]
python_version = "3.12"
